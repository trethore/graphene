package io.github.trethore.graphene.api.browser.input;

/** Identifies the physical location of a key when its location affects browser input semantics. */
public enum BrowserKeyLocation {
  STANDARD,
  LEFT,
  RIGHT,
  NUMPAD
}
